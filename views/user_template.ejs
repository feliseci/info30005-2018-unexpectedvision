<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Users</title>
</head>
<body>
<div class = "content"> <!-- Used to make footers sticky -->

<!--EJS has 2 syntaxes for rendering stuff.
    Use the = unless it's a for loop (not going to be turned to HTML). -->
<% if(user.length > 1) {
    for(i = 0; i < user.length; i++) {  %>
    <h1><%=user[i].name%></h1>

    <p>Email: <%=user[i].email%></p>
    <p>Title: <%=user[i].title%></p>
<% }} else { %>
    <h1><%=user.name%></h1>

    <p>Email: <%=user.email%></p>
    <p>Title: <%=user.title%></p>
<% } %>
</div>
</body>
</html>